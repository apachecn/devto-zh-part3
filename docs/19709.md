# 3 个鲜为人知但有用的 Vim 技巧

> 原文:[https://dev . to/Jovi ca/3-鲜为人知但很有用-vim-tips-1pbg](https://dev.to/jovica/3-little-known-but-useful-vim-tips-1pbg)

我将向你展示三个非常有用的技巧，然而，它们却鲜为人知。

* * *

## [](#tip-1-openeditsave-archives)提示#1 -打开/编辑/保存档案

运行如下命令:

```
$ vim archive.tar.gz 
```

将打开`archive.tar.gz`，并列出存档的内容。从那里，您可以打开、编辑和保存这些文件中的更改，而无需先提取它们。

多酷啊。:)

支持的档案:tar.gz，tgz，zip，jar 等。

## [](#tip-2-edit-files-remotely)提示 2——远程编辑文件

Vim 能够通过网络远程编辑文件。这个特性是 netrw 插件自带的。为了实现这一点，`netrw`使用 SSH 协议，并通过`scp`命令管理远程文件。

下面是怎么做的:

```
vim scp://user@myserver[:port]//path/to/file.txt 
```

注意远程主机上目录的双`/`，这是正确解析绝对路径所需要的。`[:port]`是可选的。

因此，使用上面的命令，您可以打开位于远程主机上的文件进行编辑。

后台实际发生的是 Vim 使用`scp`从远程机器下载请求的文件到本地`/tmp`目录，然后打开它进行编辑。

当您将更改保存到文件时，这些更改首先会应用到`/tmp`目录中的本地副本。之后，文件通过`scp`上传到远程主机。

## [](#tip-3-magic)秘诀之三——魔法

你知道吗，你可以用光标打开机器上最后编辑过的文件？

只需启动 Vim，点击`Ctrl` + `o` + `o` -就可以了。

继续敲`o`看看会发生什么；)

* * *

现在，如果你想发现更多你从未听说过的 Vim 技巧，看看我的书[快速掌握 Vim:从 WTF 到 OMG 在任何时间](http://jovicailic.org/mastering-vim-quickly/)