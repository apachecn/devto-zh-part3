# Java 数学挑战

> 原文：<https://dev.to/devit951/java-math-challenge-271g>

## 应该打印什么？

```
 int result = 0;
        result = result + (null != null ? 3 : 2);
        result += (1 & 2) == 1 ? 2 : 3;
        result += 1e5;
        result += 1e2;
        System.out.println(result); 
```

Enter fullscreen mode Exit fullscreen mode